# Public-by-design frontend values only. NEVER put the service_role key or any
# secret here. The anon key is safe in the browser only because RLS protects
# every table (verify in P02.4).
VITE_SUPABASE_URL=https://nvpbapjfeeyrbczdvjix.supabase.co
VITE_SUPABASE_ANON_KEY=paste-your-project-anon-public-key-here